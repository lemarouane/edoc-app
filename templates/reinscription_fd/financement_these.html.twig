<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Financement de la thèse - Université Abdelmalek Essaadi</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
             line-height: 1.6;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 30px;
            box-shadow: none;
        }

        .section {
            margin: 20px 0;
        }

        .section-title {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .subsection {
            margin-left: 20px;
            margin-bottom: 15px;
        }

        .form-line {
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 5px 0;
        }

        .label {
            min-width: 200px;
        }

        .dotted-line {
            flex: 1;
            height: 1.2em;
            margin: 0 5px;
            font-weight: bold;  
            text-align: justify;
        }

        .checkbox-group {
            display: flex;
            gap: 20px;
            margin: 5px 0;
        }

        .checkbox-label {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .indent {
            margin-left: 20px;
        }

        .employment-section {
            margin: 15px 0;
        }

        .employment-title {
            font-weight: bold;
            margin: 10px 0;
        }

        .co-director-section {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
<div class="section">
    <div class="section-title">Financement de la thèse :</div>
    <div class="subsection">
        <div class="section-title">Boursier :</div>
        <div class="indent">
            <div class="form-line">
                <span class="label">- Bourse de mérite, depuis :</span>
                <span class="dotted-line">
                    {% if details.bourseMerite %}
                        {{ details.bourseMeriteDepuis ? details.bourseMeriteDepuis|date('Y-m-d') : '--' }}
                    {% else %}
                        Non
                    {% endif %}
                </span>
            </div>
            <div class="form-line">
                <span class="label">- Bourse de 3<sup>ème</sup> cycle, depuis : </span>
                <span class="dotted-line">
                    {% if details.bourseTroisiemeCycle %}
                        {{ details.bourseTroisiemeCycleDepuis ? details.bourseTroisiemeCycleDepuis|date('Y-m-d') : '--' }}
                    {% else %}
                        Non
                    {% endif %}
                </span>
            </div>
            <div class="form-line">
                <span class="label">- Bourse de cotutelle. Durée : </span>
                <span class="dotted-line">
                    {% if details.bourseCotutelle %}
                        {{ details.bourseCotutelleDateDebut ? details.bourseCotutelleDateDebut|date('Y-m-d') : '--' }} 
                        au 
                        {{ details.bourseCotutelleDateFin ? details.bourseCotutelleDateFin|date('Y-m-d') : '--' }}
                    {% else %}
                        Non
                    {% endif %}
                </span>
            </div>
            <div class="form-line">
                <span class="label">- Bourse d'échange (préciser) : </span>
                <span class="dotted-line">{{ details.bourseEchange ?? '--' }}</span>
            </div>
            <div class="form-line">
                <span class="label">- Bourse dans le cadre d'un projet de recherche : </span>
                <span class="dotted-line">{{ details.bourseProjetRecherche ?? '--' }}</span>
            </div>
        </div>
    </div>

    <div class="employment-section">
        <div class="section-title">Salarié :</div>
        <div class="indent">
            <div class="form-line">
                <span class="label">Fonction :</span>
                <span class="dotted-line">{{ details.salarieFonction ?? '--' }}</span>
            </div>
            <div class="form-line">
                <span class="label">Organisme Employeur :</span>
                <span class="dotted-line">{{ details.salarieOrganisme ?? '--' }}</span>
            </div>
        </div>
    </div>

    <div class="employment-section">
        <div class="section-title">Fonctionnaire :</div>
        <div class="indent">
            <div class="form-line">
                <span class="label">Fonction :</span>
                <span class="dotted-line">{{ details.fonctionnaireFonction ?? '--' }}</span>
            </div>
            <div class="form-line">
                <span class="label">Organisme Employeur :</span>
                <span class="dotted-line">{{ details.fonctionnaireOrganisme ?? '--' }}</span>
            </div>
        </div>
    </div>
</div>

<div class="co-director-section">
    <div class="section-title">Co-Directeur de Thèse :</div>
    <div class="indent">
        <div class="form-line">
            <span class="label">- Cotutelle (Oui ou Non) :</span>
            <span class="dotted-line">{{ details.cotutelle ? 'Oui' : 'Non' }}</span>
        </div>
        {% if details.cotutelle %}
            <div class="form-line">
                <span class="label">- Université et/ou organisme de recherche:</span>
                <span class="dotted-line">{{ details.cotutelleUniversite ?? '--' }}</span>
            </div>
            <div class="form-line">
                <span class="label">- Nom et Prénom du co-directeur:</span>
                <span class="dotted-line">{{ details.cotutelleNomPrenom ?? '--' }}</span>
            </div>
            <div class="form-line">
                <span class="label">- Tél :</span>
                <span class="dotted-line">{{ details.cotutelleTelephone ?? '--' }}</span>
            </div>
            <div class="form-line">
                <span class="label">- Email :</span>
                <span class="dotted-line">{{ details.cotutelleEmail ?? '--' }}</span>
            </div>
        {% endif %}
    </div>
</div>
    </div>
</body>
</html>
